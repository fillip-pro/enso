apiVersion: v1
kind: Service
metadata:
  name: lehti-app
  labels:
    app: lehti-app
spec:
  clusterIP: "10.0.0.180"
  selector:
    app: lehti-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8090
      name: http